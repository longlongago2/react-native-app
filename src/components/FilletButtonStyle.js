import theme from '../theme';

export default {
    btn: {
        flex: 0,
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'stretch',
        height: 40,
        borderRadius: 20,
        backgroundColor: theme.theme,
    },
    btnText: {
        color: '#fff',
        fontSize: 14,
        fontWeight: 'bold',
        textAlign: 'center',
    },
};
